
### 1- Use whatweb or wafw00f to identify the web application framework: 
```powershell
 whatweb -v -a3 --log-verbose whatweb.txt <% tp.frontmatter["RHOSTS"] %>
```

### 1.1 how does the website home page looks like? 
http://<% tp.frontmatter["RHOSTS"] %> 

### 1.2 any error pages? 


### 1.3 any upload related pages? can we exploit it ? 


### 2- Run directory and file enumeration using tools like gobuster or dirb or dirsearch: 
```powershell
feroxbuster -u http://<% tp.frontmatter["RHOSTS"] %> -w /usr/share/seclists/Discovery/Web-Content/raft-small-words.txt -n -C 404 -x php
```
results 
```powershell

```

light deep search 
```powershell
feroxbuster -u http://<% tp.frontmatter["RHOSTS"] %> -w /usr/share/seclists/Discovery/Web-Content/raft-medium-words.txt -n -C 404 -x php --scan-dir-listings
```
results
```powershell

```

alternative
```powershell
dirsearch -u http://<% tp.frontmatter["RHOSTS"] %> -w /usr/share/seclists/Discovery/Web-Content/raft-medium-words.txt --exclude-status 401,302,500 -r
```
results
```powershell

```

deeper search 
```powershell
dirsearch -u http://<% tp.frontmatter["RHOSTS"] %> -w /usr/share/seclists/Discovery/Web-Content/directory-list-2.3-medium.txt --exclude-status 401,302,500 -r
```
results
```powershell

```
any interesting sub directory? 


deep search specfic directory 
```powershell
gobuster dir -u http://<% tp.frontmatter["RHOSTS"] %> -w /usr/share/seclists/Discovery/Web-Content/directory-list-2.3-medium.txt -s "200,301" -x "htm,html,php,cgi" -b ""
```
results 
```powershell

```



### 2.1 have you checked all the found links? 

### 2.2 any suspicious pages found? any worth searching information ?


### 2.3 API fuzz 
```powershell
dirsearch -u http://<% tp.frontmatter["RHOSTS"] %> -w /usr/share/wordlists/seclists/Discovery/Web-Content/api/api-endpoints.txt --exclude-status 401,302,500
```

### 3- Scan for vulnerabilities using nikto: 
```powershell
nikto -o nikto.txt --maxtime=180s -C all -h http://<% tp.frontmatter["RHOSTS"] %> 
```

### 4- HTTP subdomain Enumeration
```powershell
ffuf -u http://<% tp.frontmatter["RHOSTS"] %> -H 'Host: FUZZ.<% tp.frontmatter["VHOST"] %>' -w /usr/share/seclists/Discovery/DNS/subdomains-top1million-20000.txt -mc all -ac
```

### 5- HTTP LFI Enumeration (page= parameter)
```powershell
ffuf -w /usr/share/wordlists/seclists/Fuzzing/LFI/LFI-Jhaddix.txt -u http://<% tp.frontmatter["RHOSTS"] %>/?page=FUZZ
```


### 6- WordPress Enumeration
#### Un-authenticated
```powershell
wpscan --update --url http://<% tp.frontmatter["RHOSTS"] %> --enumerate p --plugins-detection aggressive -o websrv1/wpscan
```
#### Authenticated
```powershell
wpscan --update  --enumerate p --plugins-detection aggressive --url http://<% tp.frontmatter["RHOSTS"] %> -U USER -P pass.txt
```

### 6-1 - cmsmap (wordpress)

```powershell
wpscan --rua -e ap,at,tt,cb,dbe,u,m --url http://<% tp.frontmatter["RHOSTS"] %> --plugins-detection aggressive --api-token OUCTISvdfCBx1FEsYaHQ4veDz762n6xILBu9gvQJFgY --passwords /usr/share/wordlists/external/SecLists/Passwords/probable-v2-top1575.txt
```

### 7- Joomla Enumeration
```powershell
joomscan --ec -u <% tp.frontmatter["RHOSTS"] %>
```

### 8- Drupal Enumeration
```powershell
droopescan scan drupal http://<% tp.frontmatter["RHOSTS"] %> -t 32
```

### 9- cmsmap (WordPress, Joomla, Drupal, Moodle) Enumeration
```powershell
cmsmap -s http://<% tp.frontmatter["RHOSTS"] %> -t 2 -a "Mozilla/5.0 (Windows NT 10.0; Win64; x64; rv:69.0) Gecko/20100101 Firefox/69.0"
```


### 10 - login page found? 

### 10.1- Try the [MySQL basic commands](6-%20Zettelkasten/MySQL%20basic%20commands.md)
### 10.2- what is the default creds of that login page?
### 10.3- Form password brute-force
```powershell
# Password brute-force
hydra -V -f -l USER -P /usr/share/eaphammer/wordlists/rockyou.txt -s 80 <% tp.frontmatter["RHOSTS"] %> http-post-form "/blog/admin.php:username=^USER^&password=^PASS^:Incorrect username"
```


### 11 - searchsploit 
```powershell
searchsploit "paste here the framework tech name"
```


