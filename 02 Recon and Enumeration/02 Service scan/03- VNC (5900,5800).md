## VNC scan
```powershell 
sudo grc nmap -v -n -sV --script vnc-info,realvnc-auth-bypass,vnc-title -oA vnc -p 5900 <% tp.frontmatter["RHOSTS"] %>
```


## VNC user/password brute-force
```powershell 
hydra -V -f -L user.txt –P pass.txt -s 5900 vnc://<% tp.frontmatter["RHOSTS"] %>
```

