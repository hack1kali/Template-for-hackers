# folders names can be usernames which can be bruteforced or used with weak passwords to ssh
## 1- Brute-force(default)

```powershell
hydra -C /usr/share/seclists/Passwords/Default-Credentials/ftp-betterdefaultpasslist.txt <% tp.frontmatter["RHOSTS"] %> ftp
```

##  2- Try logging in as an anonymous user or different users you got:

```powershell
ftp <% tp.frontmatter["RHOSTS"] %>
use `anonymous` as the username and password 
```

remember: if you successfully managed to login, we can view other folders as following 
```powershell
# to bypass some firewall rules
passive 

#what is the current directory?
pwd

# cd to previous folders
cd ..

# ls 
ls 

binary # change to binary mode, we need this option to download or upload pdf, binaries...
ascii # change to ascii mode, the default mode
```

## 3- File downloads
```powershell
mkdir ftp-files
cd ftp-files
wget -m --no-passive ftp://anonymous:anonymous@<% tp.frontmatter["RHOSTS"] %>
```
###### Download all
###### If your user/password has special characters, the following command can be used:
```powershell
wget -r --user="USERNAME" --password="PASSWORD" ftp://<% tp.frontmatter["RHOSTS"] %>
```
#### grep passswords and secrets
```powershell
grep -rinE '(password|username|user|pass|key|token|secret|admin|login|credentials)'
```

## 4- Brute-force(found users name)

got a username already but no password? (if you did not get it in 15 mins, then just close it.)
```shell
hydra -V -f -l <USERS_name> -P /usr/share/wordlists/rockyou.txt ftp://<% tp.frontmatter["RHOSTS"] %> -u -vV
```

##  5- Try logging in with the users you got:

```powershell
ftp <% tp.frontmatter["RHOSTS"] %>
use `anonymous` as the username and password 
```

am i able to upload file? 
```powershell
echo "test" > test.txt 
```
connect back again in
```powershell
ftp <% tp.frontmatter["RHOSTS"] %>
use `anonymous` as the username and password 
```

```powershell
put test.txt
```
where to view it ? 


### METHODOLOGY

- Has this FTP server public exploits? Search the version in searchsploit and google it!
- Can we log in with anonymous user? Can we log in with ftp : ftp user?
- If we find an username, try easy credentials with them. For example, if we enumerate that admin is a valid username, try admin : admin, admin : password...
    - Also we can try to brute force that user. Be carefull, the machine could block us!
- If we can log in
    - What folder it is? Can we read sensitive files (configuration files with credentials, files with version numbers...)?
    - Is it a web folder? Can we upload a webshell?